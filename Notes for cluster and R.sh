Cluster
Notes for run on cluster
Try to work with one line code to run all samples

Tips:
run .sh scrpit
>make .sh version of scripts
>move it to the directory where it will run
>make it excutable % chmod u+x .sh
>run it % ./.sh

errors





R
Notes for run R
work in a fold
>creat a project fold
>creat a fold as a Kallisto_output fold, which contains all the results
>save all files, running information and R scripts on this project

Tips:
>only install package once, then only need to load this library
>setwd(path to where it will be stored)

errors
> R seesion aborted
this might be because of the incompatiable with different packages, try to load the packages in console channel
